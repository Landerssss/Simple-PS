\# 🎨 AI Simple P-Tool (简易智能P图工具)



基于 Streamlit 和 EasyOCR 开发的图片文字“即选即改”工具。

用户只需框选图片中的文字区域，系统自动识别文字和背景颜色，实现一键擦除并替换新文字。



\## ✨ 功能特点

\- \*\*区域选择\*\*: 直接在图片上框选需要修改的区域。

\- \*\*自动识别\*\*: 使用 OCR 技术自动读取原图文字。

\- \*\*智能擦除\*\*: 利用 OpenCV 修复算法自动去除原文字。

\- \*\*样式复刻\*\*: 自动吸取背景色和估算字号（支持手动微调）。



\## 🛠️ 安装与使用



\### 方法一：Windows 用户（推荐）

1\. 下载本项目（点击 Code -> Download ZIP 并解压）。

2\. 直接双击文件夹内的 \*\*`run.bat`\*\*。

3\. 等待环境自动配置完成，浏览器将自动打开工具。



\### 方法二：开发者模式

```bash

\# 1. 克隆项目

git clone https://github.com/你的用户名/你的仓库名.git



\# 2. 安装依赖

pip install -r requirements.txt



\# 3. 运行

streamlit run app.py



